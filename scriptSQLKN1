-- Поиск дубликатов пользователей по email и телефону
SELECT 
    email,
    phone,
    COUNT(*) as duplicate_count
FROM users
GROUP BY email, phone
HAVING COUNT(*) > 1;

-- Найдем уникальных пользователей
SELECT
    DISTINCT(user)
FROM users_new-- Изменила наименование сущности
